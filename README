#gameshop-demo
A demonstration MySQL database

## Usage
* connect to a MySQL server
* execute the create-schema.sql script, to create the database and tables
* execute the add-sample-data.sql script to pop
